# === Internal Development (NEVER committed to public repo) ===

# oh-my-claudecode planning and state
.omc/

# OpenSpec change management
openspec/

# Reference implementations and research repos
ref/

# Internal agent guide — force-added on `dev` only, never on `main`/public
CLAUDE.local.md

# Environment and secrets
.env
.env.*
!.env.example

# === Virtual Environments ===
.venv/
venv/

# === Build Artifacts ===

# DataDesigner default output dir (created by preview()/create() with no artifact_path,
# e.g. running templates/*.py directly). Tests use tmp dirs; this guards stray local runs.
artifacts/

# Node.js
node_modules/
dist/
*.tgz

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# === CLI Build Artifacts (generated by copy-skills.sh) ===
cli/skills/
cli/commands/

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# === Test Artifacts ===
test-workspace/
.cache/
coverage/
tests/data-agent/e2e/evals/last_run_results.json
